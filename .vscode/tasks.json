{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "dev",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "wails",
            "args": [
                "dev"
            ],
            "problemMatcher": "$go"
        }
    ]
}
